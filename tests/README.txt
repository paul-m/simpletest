// $Id$

Tests against contributed modules should reside within those module packages in
a subdir called tests. The files in this directory test core functionality,
which does not have a /tests directory. We plan to add this once this testing
framework matures a bit more.

Tests should be named and placed according to the standard:
http://drupal.org/node/231348.
